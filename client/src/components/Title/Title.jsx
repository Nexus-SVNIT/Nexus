import React from 'react'

const Title = ({ children }) => {
    return (
        <h1 className='uppercase flex items-center justify-center text-[2.25rem]'>{children}</h1>

    )
}

export default Title